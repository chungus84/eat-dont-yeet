export default class Profile {
    constructor(id, userId, userName, accessToken) {
        this.id = id;
        this.userId = userId;
        this.userName = userName;
        this.accessToken = accessToken;
    }
}
